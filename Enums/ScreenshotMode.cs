namespace CameraRecordingService.Enums
{
    /// <summary>
    /// Screenshot capture modes
    /// </summary>
    public enum ScreenshotMode
    {
        /// <summary>
        /// Capture the entire screen
        /// </summary>
        FullScreen,

        /// <summary>
        /// Allow user to select a region to capture
        /// </summary>
        RegionSelection
    }
}
